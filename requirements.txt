# HormuzWatch — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Core data ---
requests==2.31.0
pandas==2.1.4
numpy==1.26.2

# --- Database ---
supabase==2.3.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# --- Warehouse (Phase Layer 3 — dbt star schema in Supabase Postgres) ---
dbt-postgres~=1.7.0

# --- Environment ---
python-dotenv==1.0.0

# --- Dashboard ---
streamlit==1.29.0
plotly==5.18.0
folium==0.15.1
streamlit-folium==0.17.0

# --- Analytics & ML ---
scikit-learn==1.3.2
statsmodels==0.14.1
scipy==1.11.4
xgboost==2.0.3

# --- MLOps (Phase Layer 5 — experiment tracking + model registry) ---
mlflow==2.9.2

# --- NLP (Phase 4) ---
transformers==4.36.2
torch==2.1.2

# --- GenAI (Phase Layer 6 — RAG assistant, structured extraction, daily briefing) ---
# google-genai, not the deprecated google-generativeai package.
google-genai>=1.0.0
pydantic>=2.0.0

# --- API backend (Phase 6) ---
fastapi==0.108.0
uvicorn==0.25.0
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# --- Scheduling ---
schedule==1.2.1

# --- Utilities ---
tqdm==4.66.1
loguru==0.7.2

# --- Testing (Phase Layer 5 — CI) ---
pytest==7.4.4
